local repo = "https://raw.githubusercontent.com/Clehxb/Clematis_Loader/main/"
if getgenv().DoIntro == true then
    spawn(loadstring(game:HttpGet(repo .. "Withintro"))())
else
    spawn(loadstring(game:HttpGet(repo .. "NoIntro"))())
end
